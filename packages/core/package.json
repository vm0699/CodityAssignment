{
  "name": "@pulse/core",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Shared domain logic and persistence layer: schema migrations, repositories, retry strategies, job state machine, cron utilities, event bus.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "migrate": "tsx src/migrate-run.ts",
    "seed": "tsx src/seed.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cron-parser": "^4.9.0",
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.6",
    "@types/pg": "^8.11.10"
  }
}
